[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    contract: validates a versioned contract shared with another component
    hardware: requires a physical scale, printer, or serial port
    integration: crosses process or service boundaries
